import { Button } from "@mui/material";

export const HomePage = () => {

  const handleClick = async () => {
  };

  return (
    <Button
      onClick={handleClick}
    >
      Logout
    </Button>
  )
};
